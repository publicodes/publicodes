valid rule name :

  $ publicodes compile nom.publicodes  -t debug_eval_tree -o -
  a :
  get_context(a)
  
  arrondi à 0.5 près :
  get_context(arrondi à 0.5 près)
  
  arrondi à 2 décimales :
  get_context(arrondi à 2 décimales)
  
  arrondi à l'unité :
  get_context(arrondi à l'unité)
  
  garde-côte :
  get_context(garde-côte)
  
  scénario T+3°C :
  get_context(scénario T+3°C)
