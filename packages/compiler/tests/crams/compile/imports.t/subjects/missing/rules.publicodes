module missing:
  importer: module missing

module empty:
  importer: ./module empty

package missing:
  valeur: rule vendored a + 3
  importer:
    package: package missing
    module: vendored a

vendored missing:
  valeur: rule vendored c + 3
  importer:
    package: package a
    module: vendored missing

key missing:
  valeur: rule vendored c + 3
  importer:
    package: package a
