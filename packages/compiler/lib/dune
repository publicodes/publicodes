(library
 (inline_tests)
 (name compiler)
 (public_name publicodes.compiler)
 (libraries
  yaml
  base
  utils
  parser
  resolver
  typed_tree
  hashed_tree
  yaml_parser
  dependency_graph
  replacements)
 (modules :standard)
 (preprocess
  (pps ppx_inline_test ppx_deriving.show ppx_compare sedlex.ppx ppx_assert)))

(rule
 (with-stdout-to
  js_template.ml
  (progn
   (echo "let template = {|")
   (cat ../templates/runtime.js)
   (cat ../templates/template.js)
   (echo "|}"))
))

(rule
 (with-stdout-to
  eval_tree_template.ml
  (progn
   (echo "let template = {|")
   (cat ../templates/template.eval_tree)
   (echo "|}"))
))
