(library
 (name utils)
 (public_name publicodes.utils)
 (libraries base stdune ppx_deriving jingoo fpath)
 (modules :standard)
 (preprocess
  (pps ppx_sexp_conv ppx_deriving.show ppx_compare)))

(rule
 (with-stdout-to
  default.ml
  (progn
   (echo "let publicodespath = \"$PUBLICODESPATH\""))
))
